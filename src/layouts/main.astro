---
import { defineStyleVars } from "astro:css";
import '../styles/global.css';
---

// Define custom font variables
const nunitoFont = defineStyleVars({
  "--font-nunito": "Nunito, sans-serif",
});

const ptSansFont = defineStyleVars({
  "--font-pt-sans": "PT Sans, sans-serif",
});

<html lang="en">
  <head>
    <title>Create Next App</title>
    <meta name="description" content="Generated by create next app" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito&family=PT+Sans:wght@400;700&display=swap" />
  </head>
  <body class="antialiased relative" style={`${nunitoFont} ${ptSansFont}`}>
    <div class="texture"></div>
    <slot />
  </body>
</html>